[project]
name = "attct"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = [
    "torch",
    "transformers",
    "peft",
    "datasets",
    "wandb",
    "tqdm",
    "pyyaml",
    "vllm",
    "bitsandbytes",
    "httpx",
    "sympy<1.13",
]

[dependency-groups]
dev = [
    "pytest>=9.0.2",
]

model:
  output_attentions: false      # ACT doesn't use attention weights
  output_hidden_states: true    # ACT requires hidden states

loss:
  name: "ActivationConsistencyLoss"
  weight: 1.0
  kwargs:
    layer_selection: "all"
    normalize: false

model:
  output_attentions: false
  output_hidden_states: true   # ACT requires hidden states

loss:
  name: "ActivationConsistencyLoss"
  weight: 0.0001               # 1e-4 from paper
  kwargs:
    layer_selection: "all"
    normalize: false

data:
  source: sycophancy_bct
  mode: sycophancy

training:
  save_dir: "/workspace/checkpoints/act_sycophancy"  # persist to network volume
